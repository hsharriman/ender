need to know what triangles are captured
for SAS, ASA, RHL, need a concept of rotation around a triangles
for angles, often need concept of inner vs outer angle (always taking the smaller of the 2 angles)
don't have way to describe circles, arcs, inscribed stuff
don't have to describe things algebraically
for each reason there should be a way to generically describe the relationship between requirements
that can be checked for each reason
incorrect steps are checked based on: 1. enough prior steps? 2. correct stmt type concluded?
3. is relationship between stmt and requirements met?
Defining relationships between stmts and requirements for a reason:
would ideally be pretty generic... 

1:1:
Reflexive

many reqs, 1 conclusion:
SAS/ASA (write methods for SAS/ASA/AAS and attach equivalence methods to each object type?)
what about when the comparisons aren't to create congruent/similar things (i.e. some relationship between circle + line)
or circumcenter/incenter/centroid relationships

centroid: conclude point == centroid if segments w/in triangle all bisect opposite sides and P intersects w all 3
  reason centroid(bisect, bisect, bisect) -> centroid(p: Point)
centroid (converse): if point == centroid, all segments within triangle bisect
  reason centroid_c(p_centroid) -> bisect(?????)
  // issue here is that there are 3 different segments that could be concluded to be bisecting
some construction steps might be necessary here (like stating that certain lines/points are collinear/intersecting, invisible (or hidden by default) for a proof)

// reasons and conclusions can match or be different
perpendicular(con_adj_ang) -> perpendicular(s1: Segment, s2: Segment)


// reasons involving angles:
reason supplementary() -> 

1 req, many conclusions:
cpctc

many reqs, many conclusions:


