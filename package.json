{
  "name": "ender",
  "homepage": "https://hsharriman.github.io/ender",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "ender-interface",
    "ender-proof-core",
    "geometry-object"
  ],
  "scripts": {
    "start": "npm --workspace ender-interface start",
    "build": "npm --workspace ender-interface build",
    "test": "npm --workspace ender-interface test",
    "lint": "echo \"No root lint configured\"",
    "test-core": "npm --workspace ender-proof-core test",
    "build-geometry-object": "npm --workspace geometry-object run build"
  }
}
